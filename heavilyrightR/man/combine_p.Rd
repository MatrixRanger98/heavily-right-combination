% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/combination.R
\name{combine_p}
\alias{combine_p}
\title{Combine p-values}
\usage{
combine_p(p, spec = NULL, ..., decision = FALSE)
}
\arguments{
\item{p}{Numeric p-values. For batched input, studies occupy the final
dimension.}

\item{spec}{A specification from \code{\link[=new_combination]{new_combination()}}.}

\item{...}{Arguments forwarded to \code{\link[=new_combination]{new_combination()}} when \code{spec} is absent.}

\item{decision}{If \code{TRUE}, return whether the score reaches the configured
rejection cutoff instead of a global p-value.}
}
\value{
Global p-values or logical rejection decisions.
}
\description{
Combine p-values
}
\examples{
rule <- new_combination("hcauchy", m = 3, calibration = "finite")
p <- c(0.01, 0.2, 0.4)
combine_p(p, rule)
combine_p(p, rule, decision = TRUE)
combine_p(rbind(p, c(0.2, 0.5, 0.8)), rule)
}
\seealso{
\code{\link[=new_combination]{new_combination()}}, \code{\link[=combination_score]{combination_score()}}
}
