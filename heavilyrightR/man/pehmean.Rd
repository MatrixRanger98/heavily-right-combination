% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/null-laws.R
\name{pehmean}
\alias{pehmean}
\alias{dehmean}
\alias{qehmean}
\title{Finite-number weighted reciprocal-score null law}
\usage{
pehmean(q, weights, lower.tail = TRUE, log.p = FALSE, diagnostics = FALSE)

dehmean(x, weights, log = FALSE, diagnostics = FALSE)

qehmean(p, weights, lower.tail = TRUE, log.p = FALSE)
}
\arguments{
\item{weights}{A positive integer for mathematical equal weights, or a
nonnegative weight vector whose sum is one within numerical tolerance.
Vectors are not renormalized. Zero weights are removed. For reciprocal
scores, a count has support exactly one; an explicit vector has support
at the exact real sum of its supplied binary floating-point entries.}

\item{lower.tail, log.p}{Standard R distribution arguments.}

\item{diagnostics}{Return a diagnostic list instead of a bare value.}

\item{x, q}{Numeric scalar score.}

\item{log}{Return log density.}

\item{p}{CDF probability.}
}
\value{
A numeric value or, for density/probability calls with diagnostics
enabled, a list with \code{value}, \code{estimated_error}, and \code{message}. Error
estimates remain on the unlogged scale. Quantiles are scalar.
}
\description{
These functions evaluate the independence law of a weighted sum of \code{1/U}
for independent uniform p-values. A scalar count has support boundary one;
a supplied vector has support at the sum of its entries, without
renormalization. Support distance is evaluated with compensated summation.
Numerical and diagnostic conventions are the same as \code{\link[=phcmean]{phcmean()}}.
}
\examples{
pehmean(5, weights = 2, lower.tail = FALSE, diagnostics = TRUE)
dehmean(2, weights = 1)
threshold <- qehmean(0.95, weights = 2)
pehmean(threshold, weights = 2)
pehmean(2, weights = 100, diagnostics = TRUE)  # Stable lower-tail CDF
}
\seealso{
\code{\link[=phcmean]{phcmean()}}, \code{\link[=new_combination]{new_combination()}}
}
