% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/multivariate.R
\name{fit_meta_md}
\alias{fit_meta_md}
\title{Fit a multidimensional heavily-right confidence region}
\usage{
fit_meta_md(
  studies,
  parameter_dim = NULL,
  method = "hcauchy",
  weights = NULL,
  alpha = 0.05,
  calibration = c("auto", "finite", "landau"),
  empty_policy = NULL
)
}
\arguments{
\item{studies}{List of \code{\link[=study]{study()}} objects.}

\item{parameter_dim}{Common parameter dimension, including dimension one.}

\item{method, weights, alpha, calibration}{Arguments to \code{\link[=new_combination]{new_combination()}}.}

\item{empty_policy}{Optional \code{\link[=empty_set_policy]{empty_set_policy()}}.}
}
\value{
A \code{heavilyright_md_fit}. Certified empty sets and unresolved numerical
cases raise distinct typed errors with complete \code{diagnostics} records.
Successful fits contain retained \code{studies}, \code{specification}, \code{minimum},
and \code{empty_set_diagnostics}. \code{coef()} extracts the minimum-score point;
use \code{\link[=support_interval]{support_interval()}} for directional bounds, not \code{confint()}.
}
\description{
Study removal is disabled by default. An opt-in policy removes the entire
input block with the smallest individual p-value at a validated minimum,
skipping deletions that lose parameter identifiability. Every removal needs
a convex lower bound above the cutoff. The procedure is a sensitivity
analysis, not a post-selection coverage correction.
}
\details{
Multi-study fits support \code{"hcauchy"} and \code{"ehmp"} only. For
Hotelling study dimension \code{q}, both require \code{df >= 1} when \code{q = 1};
EHMP requires \code{df >= q} when \code{q >= 2}; HCauchy requires \code{df >= 2.5}
when \code{q = 2} and \code{df >= q + 1} when \code{q >= 3}. Known-covariance blocks
have no df restriction. The combined projection matrices must span the
common parameter space. Independence calibration does not justify treating
dependent study blocks as independent.
}
\examples{
studies <- list(
  study(c(0.2, -0.1), diag(c(0.04, 0.09))),
  study(c(0.1, 0), diag(c(0.05, 0.08)))
)
fit <- fit_meta_md(studies)
coef(fit)
contains(fit, coef(fit))
support_interval(fit, c(1, -1))
}
\seealso{
\code{\link[=study]{study()}}, \code{\link[=support_interval]{support_interval()}}, \code{\link[=empty_set_policy]{empty_set_policy()}}
}
