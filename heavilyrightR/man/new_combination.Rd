% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/combination.R
\name{new_combination}
\alias{new_combination}
\title{Define a p-value combination rule}
\usage{
new_combination(
  method = "hcauchy",
  m = NULL,
  weights = NULL,
  alpha = 0.05,
  calibration = c("auto", "finite", "landau")
)
}
\arguments{
\item{method}{Combination method: \code{"hcauchy"}, \code{"ehmp"}, \code{"hmp"},
\code{"cauchy"}, \code{"levy"}, \code{"fisher"}, \code{"stouffer"}, \code{"bonferroni"}, or
\code{"simes"}.}

\item{m}{Number of studies when \code{weights} is omitted.}

\item{weights}{Strictly positive weights summing to one.}

\item{alpha}{Significance level.}

\item{calibration}{\code{"finite"} for the finite-number independence law,
\code{"landau"} for its asymptotic approximation, or \code{"auto"} (finite through
1,000 studies). The returned specification records the resolved choice.}
}
\value{
A \code{heavilyright_combination} specification.
}
\description{
Define a p-value combination rule
}
\details{
Finite HCauchy/EHMP calibration assumes independent uniform null
p-values. HMP always uses the Landau approximation. Other rules use their
method-specific calibration; choosing a rule does not validate dependence
assumptions. Fisher uses equal weights, warning if unequal weights are
supplied. With one study every rule returns the original p-value.
}
\examples{
rule <- new_combination("hcauchy", weights = c(0.2, 0.3, 0.5),
                        calibration = "finite")
rule
combine_p(c(0.01, 0.2, 0.4), rule)
}
\seealso{
\code{\link[=combine_p]{combine_p()}}, \code{\link[=combination_score]{combination_score()}}, \code{\link[=combination_cutoff]{combination_cutoff()}}
}
