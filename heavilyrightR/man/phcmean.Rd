% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/null-laws.R
\name{phcmean}
\alias{phcmean}
\alias{dhcmean}
\alias{qhcmean}
\title{Finite-number weighted half-Cauchy null law}
\usage{
phcmean(q, weights, lower.tail = TRUE, log.p = FALSE, diagnostics = FALSE)

dhcmean(x, weights, log = FALSE, diagnostics = FALSE)

qhcmean(p, weights, lower.tail = TRUE, log.p = FALSE)
}
\arguments{
\item{weights}{A positive integer for mathematical equal weights, or a
nonnegative weight vector whose sum is one within numerical tolerance.
Vectors are not renormalized. Zero weights are removed. For reciprocal
scores, a count has support exactly one; an explicit vector has support
at the exact real sum of its supplied binary floating-point entries.}

\item{lower.tail, log.p}{Standard R distribution arguments.}

\item{diagnostics}{Return a diagnostic list instead of a bare value.}

\item{x, q}{Numeric scalar score.}

\item{log}{Return log density.}

\item{p}{CDF probability.}
}
\value{
A number, or a diagnostic list with \code{value}, \code{estimated_error}, and
\code{message} when requested. The error is on the unlogged probability/density
scale even when \code{log.p} or \code{log} is true; it is not a rigorous error bound.
Quantiles accept one probability strictly between zero and one after
applying \code{lower.tail} and \code{log.p}.
}
\description{
These functions numerically invert the finite-number independence law. The
calculation is numerical rather than exact arithmetic. Set \code{diagnostics}
to receive the numerical error estimate. Failed quadrature raises a typed
condition. Lower tails use positive convolution or normalized Laplace
inversion, with direct CDF evaluation. Analytic support boundaries and
bounded underflow can return zero without quadrature; bounded underflow
retains a positive error allowance. See the mathematical-methods vignette.
}
\examples{
phcmean(5, weights = c(0.2, 0.3, 0.5),
        lower.tail = FALSE, diagnostics = TRUE)
dhcmean(2, weights = 1)
threshold <- qhcmean(0.95, weights = 2)
phcmean(threshold, weights = 2)
phcmean(0.01, weights = 10, diagnostics = TRUE)  # A tiny direct CDF
}
\seealso{
\code{\link[=pehmean]{pehmean()}}, \code{\link[=new_combination]{new_combination()}}
}
