% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/workflows.R
\name{nma_contrasts}
\alias{nma_contrasts}
\title{Certified treatment contrasts from a fitted network}
\usage{
nma_contrasts(object, comparisons = NULL, maxeval = 2000L)
}
\arguments{
\item{object}{A result of \code{\link[=fit_nma]{fit_nma()}}.}

\item{comparisons}{Optional two-column matrix/data frame of treatment labels;
defaults to all unordered pairs including the reference.}

\item{maxeval}{Maximum optimizer evaluations per stage.}
}
\value{
A data frame of contrast estimates, lower/upper simultaneous support
bounds and widths. Its \code{intervals} attribute retains full certificates.
Any uncertified contrast raises an error carrying completed results.
}
\description{
Certified treatment contrasts from a fitted network
}
\examples{
network <- network_studies(c(0.2, 0.25, 0.1), c(0.2, 0.18, 0.22),
  treat1 = c("B", "C", "C"), treat2 = c("A", "A", "B"), reference = "A")
fit <- fit_nma(network)
result <- nma_contrasts(fit, comparisons = rbind(c("B", "A"), c("C", "B")))
result
attr(result, "intervals")[[1]]$upper_diagnostics
}
\seealso{
\code{\link[=fit_nma]{fit_nma()}}, \code{\link[=support_interval]{support_interval()}}
}
