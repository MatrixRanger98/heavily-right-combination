% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/workflows.R
\name{fit_dac}
\alias{fit_dac}
\title{Fit a general divide-and-combine confidence region}
\usage{
fit_dac(
  sample,
  blocks = NULL,
  block_size = NULL,
  method = "hcauchy",
  weights = NULL,
  alpha = 0.05,
  calibration = c("auto", "finite", "landau"),
  empty_policy = NULL
)
}
\arguments{
\item{sample}{Finite numeric observation-by-coordinate matrix.}

\item{blocks}{List of coordinate indices or column names forming a partition.}

\item{block_size}{Positive integer for consecutive blocks; the final block
may be smaller. Omit both block arguments for singleton coordinate blocks.}

\item{method, weights, alpha, calibration, empty_policy}{Arguments to \code{\link[=fit_meta_md]{fit_meta_md()}}.}
}
\value{
A multidimensional fit usable with \code{\link[=contains]{contains()}}, \code{\link[=score]{score()}}, and
\code{\link[=support_interval]{support_interval()}}, retaining the coordinate partition and sample size.
}
\description{
Fit a general divide-and-combine confidence region
}
\examples{
set.seed(21)
x <- matrix(rnorm(120), 30, 4)
fit <- fit_dac(x, block_size = 2)
coef(fit)
contains(fit, colMeans(x))
support_interval(fit, c(1, 0, -1, 0))
}
\seealso{
\code{\link[=dac_studies]{dac_studies()}}, \code{\link[=fit_meta_md]{fit_meta_md()}}
}
