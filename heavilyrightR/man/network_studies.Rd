% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/workflows.R
\name{network_studies}
\alias{network_studies}
\title{Construct a general contrast-based treatment network}
\usage{
network_studies(
  estimate,
  se = NULL,
  treat1,
  treat2,
  study_id = NULL,
  reference = NULL,
  treatments = NULL,
  vcov = NULL,
  df = NULL,
  multiarm = c("require_covariance", "independent")
)
}
\arguments{
\item{estimate}{Finite contrast estimates.}

\item{se}{Positive contrast standard errors when \code{vcov} is omitted.}

\item{treat1, treat2}{Treatment labels for each contrast.}

\item{study_id}{Trial identifiers; omitted identifiers make rows independent.}

\item{reference}{Reference treatment; defaults to the first observed treatment.}

\item{treatments}{Optional complete treatment order, including reference.}

\item{vcov}{List of positive-definite covariance matrices, one per unique
study identifier in first-appearance order; each follows that trial's rows.}

\item{df}{\code{NULL} for known covariance or a scalar/vector of Hotelling degrees
of freedom, one per resulting study block.}

\item{multiarm}{\code{"require_covariance"} or explicit \code{"independent"} row model.}
}
\value{
A \code{heavilyright_network} carrying study summaries and treatment maps.
}
\description{
A positive estimate means \code{treat1 - treat2}. Repeated study identifiers
require a covariance matrix for their jointly modelled contrasts. Use an
independent, nonredundant contrast basis for each multivariate trial. The
explicit \code{multiarm = "independent"} option instead treats every row as a
separate scalar test in an independent-comparison model;
it does not correct dependence within a multi-arm trial.
}
\examples{
network <- network_studies(
  c(0.2, 0.25, 0.1), c(0.2, 0.18, 0.22),
  treat1 = c("B", "C", "C"), treat2 = c("A", "A", "B"),
  reference = "A"
)
nma_wls(network)
# Two nonredundant contrasts from one three-arm trial:
joint <- network_studies(c(0.2, 0.25),
  treat1 = c("B", "C"), treat2 = c("A", "A"),
  study_id = c("trial-1", "trial-1"), reference = "A",
  vcov = list(matrix(c(0.04, 0.01, 0.01, 0.05), 2)))
joint$studies[[1]]$vcov
}
\seealso{
\code{\link[=network_arm_studies]{network_arm_studies()}}, \code{\link[=fit_nma]{fit_nma()}}, \code{\link[=nma_wls]{nma_wls()}}
}
