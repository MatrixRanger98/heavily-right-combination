% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/workflows.R
\name{network_arm_studies}
\alias{network_arm_studies}
\title{Construct correlated contrasts from independent treatment arms}
\usage{
network_arm_studies(
  mean,
  se,
  treatment,
  study_id,
  reference = NULL,
  treatments = NULL,
  df = NULL
)
}
\arguments{
\item{mean, se}{Arm means and positive standard errors of the means.}

\item{treatment, study_id}{Treatment and trial identifiers per arm.}

\item{reference}{Reference treatment; defaults to the first observed treatment.}

\item{treatments}{Optional complete treatment order, including reference.}

\item{df}{\code{NULL} for known covariance or a scalar/vector of Hotelling degrees
of freedom, one per resulting study block.}
}
\value{
A \code{heavilyright_network} for \code{\link[=fit_nma]{fit_nma()}}.
}
\description{
Within each trial the first arm is the local baseline. Covariances include
the shared baseline variance. Inputs are means and standard errors of arm
means, not standard deviations. Known/asymptotic covariance is the default;
supplying Hotelling \code{df} requires a scientifically justified common covariance
estimator and is not inferred from unequal arm sample sizes.
}
\examples{
network <- network_arm_studies(
  mean = c(1, 1.2, 0.8, 1, 1.15), se = rep(0.1, 5),
  treatment = c("A", "B", "C", "A", "B"),
  study_id = c("trial1", "trial1", "trial1", "trial2", "trial2"),
  reference = "A"
)
network$studies[[1]]$vcov  # Shared-baseline off-diagonal covariance
nma_wls(network)
}
\seealso{
\code{\link[=network_studies]{network_studies()}}, \code{\link[=fit_nma]{fit_nma()}}
}
