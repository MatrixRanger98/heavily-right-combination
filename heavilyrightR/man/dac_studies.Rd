% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/workflows.R
\name{dac_studies}
\alias{dac_studies}
\title{Construct coordinate-block studies for divide-and-combine inference}
\usage{
dac_studies(sample, blocks = NULL, block_size = NULL)
}
\arguments{
\item{sample}{Finite numeric observation-by-coordinate matrix.}

\item{blocks}{List of coordinate indices or column names forming a partition.}

\item{block_size}{Positive integer for consecutive blocks; the final block
may be smaller. Omit both block arguments for singleton coordinate blocks.}
}
\value{
A list of \code{\link[=study]{study()}} objects with block and sample metadata.
}
\description{
Rows are observations and columns are parameters. Blocks partition columns,
not observations. Each block uses the sample mean, covariance of that mean
\verb{W/[n(n-1)]}, and Hotelling degrees of freedom \code{n-1}. The finite combination
calibration assumes independent block p-values; splitting correlated columns
does not itself establish that assumption or guarantee nominal coverage.
}
\examples{
set.seed(21)
x <- matrix(rnorm(120), 30, 4, dimnames = list(NULL, letters[1:4]))
blocks <- dac_studies(x, blocks = list(first = c("a", "c"),
                                     second = c("b", "d")))
blocks[[1]]$vcov
blocks[[1]]$projection
}
\seealso{
\code{\link[=fit_dac]{fit_dac()}}, \code{\link[=covariance_of_mean]{covariance_of_mean()}}
}
