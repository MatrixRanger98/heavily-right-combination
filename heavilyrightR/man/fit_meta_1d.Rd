% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/univariate.R
\name{fit_meta_1d}
\alias{fit_meta_1d}
\title{Fit a one-dimensional heavily-right meta-analysis}
\usage{
fit_meta_1d(
  estimate,
  se,
  df = NULL,
  method = "hcauchy",
  weights = NULL,
  alpha = 0.05,
  calibration = c("auto", "finite", "landau"),
  empty_policy = NULL,
  study_labels = NULL
)
}
\arguments{
\item{estimate}{Study estimates.}

\item{se}{Positive study standard errors.}

\item{df}{\code{NULL} for normal calibration or positive Student-t degrees of
freedom.}

\item{method, weights, alpha, calibration}{Combination-rule arguments.}

\item{empty_policy}{Optional policy from \code{\link[=empty_set_policy]{empty_set_policy()}}. Omission keeps
study removal off.}

\item{study_labels}{Optional stable study identifiers.}
}
\value{
A \code{heavilyright_ci} object with \code{lower}, \code{upper}, \code{estimate},
\code{empty}, \code{specification}, \code{minimum}, and \code{empty_set_diagnostics}.
\code{coef()} extracts the estimate and \code{confint()} extracts the endpoints.
Always inspect \code{empty}: when true, equal stored endpoints are a sentinel
for an empty set, not a singleton confidence interval. Refit to change
the confidence level. Numerical failures raise an error.
}
\description{
Fit a one-dimensional heavily-right meta-analysis
}
\details{
Multiple-study HCauchy/EHMP inference with Student-t calibration
requires all degrees of freedom to be at least one. Other scalar
combination rules do not carry the same convexity guarantee.
}
\examples{
fit <- fit_meta_1d(c(0.10, 0.14, 0.08), c(0.04, 0.05, 0.03))
fit$empty
coef(fit)
confint(fit)
fit_meta_1d(0.2, se = 0.1, df = 19)  # One-study Student-t interval
}
\seealso{
\code{\link[=empty_set_policy]{empty_set_policy()}}, \code{\link[=fit_meta_md]{fit_meta_md()}}, \code{\link[=new_combination]{new_combination()}}
}
