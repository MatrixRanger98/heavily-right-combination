% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/multivariate.R
\name{support_interval}
\alias{support_interval}
\title{Directional support interval for a fitted confidence region}
\usage{
support_interval(object, direction, maxeval = 2000L)
}
\arguments{
\item{object}{A fit from \code{\link[=fit_meta_md]{fit_meta_md()}}, \code{\link[=fit_nma]{fit_nma()}}, or \code{\link[=fit_dac]{fit_dac()}}.}

\item{direction}{Finite nonzero parameter-space direction.}

\item{maxeval}{Maximum evaluations per constrained optimizer stage.}
}
\value{
A \code{heavilyright_support_interval} with \code{lower}, \code{upper},
\code{lower_point}, \code{upper_point}, \code{success}, \code{lower_diagnostics},
\code{upper_diagnostics}, \code{minimum_diagnostics}, and \code{empty_set_diagnostics}.
A failed certificate can return \code{success = FALSE}; numerical failures
can instead raise a \code{heavilyright_numerical_error}. Neither outcome
establishes an empty confidence region.
}
\description{
This projects the entire confidence region; it is not a line slice.
Check \code{success} before using numerical endpoints. Every success requires
independent boundary, positive-multiplier KKT/subgradient, and physical-unit
reconstruction checks. These residual checks are not interval arithmetic.
}
\examples{
fit <- fit_meta_md(list(study(c(0.2, -0.1), diag(c(0.04, 0.09)))))
interval <- support_interval(fit, c(1, -1))
stopifnot(interval$success)
c(lower = interval$lower, upper = interval$upper)
interval$upper_diagnostics
}
\seealso{
\code{\link[=fit_meta_md]{fit_meta_md()}}, \code{\link[=nma_contrasts]{nma_contrasts()}}
}
